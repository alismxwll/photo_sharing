<%= form_for @post, :url => posts_path, :html => { :multipart => true } do |f| %>
  <%= f.label "Add Discription: " %>
  <%= f.text_field :description %>
  <%= f.file_field :image %>
  <%= f.submit %>
<% end %>
